import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'blankProyect',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
